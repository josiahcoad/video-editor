- Would allow them to see the definition document and contribute to it.

We could create a webapp that surfaces ideas for content creation.
Maybe categorizes into sessions (at least allow tagging by session and grouping by)
Show the virality score as a progress bar
Color code pillars (and link back to the definition document)
Color code ICP (and link back to the definition document)
Allow users to select the ideas and add them to a session